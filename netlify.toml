[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime — enables SSR, API routes, ISR, and next/image.
[[plugins]]
  package = "@netlify/plugin-nextjs"
